{% extends 'base.html.twig' %}

{% block title %}TypeEtablissement{% endblock %}

{% block body %}
    <h1>TypeEtablissement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_etablissement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_etablissement.nom }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ type_etablissement.createdAt ? type_etablissement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ type_etablissement.updatedAt ? type_etablissement.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_etablissement_index') }}">back to list</a>

    <a href="{{ path('app_type_etablissement_edit', {'id': type_etablissement.id}) }}">edit</a>

    {{ include('type_etablissement/_delete_form.html.twig') }}
{% endblock %}
