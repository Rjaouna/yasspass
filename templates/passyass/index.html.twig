{% extends 'base.html.twig' %}

{% block title %}Valider le code promo{% endblock %}

{% block body %}
<div class="container-fluid mt-5">
    <!-- Card for Promo Pass -->
    <div class="card text-center shadow-lg border-light" style="background-color: #f9f9f9; border-radius: 15px;">
        <div class="card-body">
            <!-- Promo Icon with Celebration -->
            <div class="mb-4">
                <i class="bi bi-stars text-warning fs-1"></i> <!-- Icône de célébration -->
                <i class="bi bi-check-circle-fill text-success fs-1 mx-2"></i> <!-- Icône de validation -->
                <i class="bi bi-stars text-warning fs-1"></i> <!-- Icône de célébration -->
            </div>
            <!-- Félicitations -->
            <h3 class="card-title fw-bold text-primary" style="font-size: 2rem;">🎉 Félicitations ! 🎉</h3>
            <p class="card-text mt-3" style="font-size: 1.1rem;">
                Votre <span class="fw-bold text-success">code promo</span> a été validé avec succès ! Profitez dès maintenant de votre <span class="fw-bold">réduction exclusive </span> sur votre commande. 🎁
            </p>
            <p class="card-text text-muted" style="font-size: 1rem;">
                <i class="bi bi-info-circle"></i> Ce pass est <span class="fw-bold">valide une fois par jour</span>, alors ne manquez pas cette opportunité !
            </p>
            <!-- Compteur -->
            <p class="mt-4">
                <span class="fw-bold text-danger fs-3" style="font-size: 2rem; color: #ff5733;"><span id="counter" class="fw-bold text-danger" style="font-size: 2.5rem;">10</span></span>
            </p>
        </div>
    </div>
</div>

<script>
    // Compteur pour la redirection
    let countdown = 10;
    const counterElement = document.getElementById("counter");

    setInterval(function() {
        countdown--;
        counterElement.textContent = countdown;
        if (countdown <= 0) {
            window.location.href = "/"; // Remplacez par votre URL de redirection
        }
    }, 1000);
</script>

{% endblock %}
