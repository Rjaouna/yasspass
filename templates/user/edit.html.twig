{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
<section class="py-5" style="background-color: #ffffff;">
    <div class="container-fluid h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col col-xl-10">
                <div class="card" style="border-radius: 1rem;">
                    <div class="row g-0">
                        <!-- Section image -->
                        <div class="col-md-6 col-lg-5 d-flex align-items-center justify-content-center">
                            <i class="fas fa-user fa-5x"></i>

                        </div>
                        <!-- Section formulaire -->
                        <div class="col-md-6 col-lg-7 d-flex align-items-center">
                            <div class="card-body p-4 p-lg-5 text-black">
                                {{ form_start(form, {'attr': {'class': 'user-form'}}) }}

                                <h5 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px;">Mon profil</h5>

                                <div class="form-floating mb-3">
    {{ form_widget(form.email, {'attr': {'class': 'form-control', 'placeholder': 'Email', 'readonly': 'readonly'}}) }}
    <label for="email">Email</label>
</div>



                                <div class="form-floating mb-3">
                                    {{ form_widget(form.etablissement, {'attr': {'class': 'form-control'}}) }}
                                    <label for="etablissement">Établissements</label>
                                </div>

                                <div class="pt-1 mb-4">
                                    <button class="btn btn-primary btn-lg btn-block w-100" type="submit">Enregistrer</button>
                                </div>

                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
