<!--<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <!-- Nom de la marque -->
    <a class="navbar-brand" href="{{ path('app_homepage') }}">Pass Yass</a>

    <!-- Bouton pour navigation mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Contenu de la navigation -->
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_homepage') }}">Home
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Etablissement</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="{{ path('app_etablissement_index') }}">Liste des établissements</a>
            <a class="dropdown-item" href="{{path('app_type_etablissement_index')}}">Type Etablissement</a>
            {# <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="{{ path('app_settings') }}">Thème</a> #}
          </div>
        </li>
      </ul>

      <!-- Dropdown aligné à droite -->
      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
            Mon Compte
          </a>
          <div class="dropdown-menu dropdown-menu-end">
          {% if app.user %}
            {# <a class="dropdown-item" href="{{ path('app_user_edit', {'id': app.user.id}) }}">Profil</a> #}
            <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
            <div class="dropdown-divider"></div>
            {% else %}
            <a class="dropdown-item" href="{{ path('app_register') }}">Créer un compte</a>

          {% endif %}


          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>-->
